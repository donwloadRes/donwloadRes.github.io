---
layout: post
title: "arm64 Docker 安装包"
date:   2023-01-24
tags: [Docker,arm64,install,sh,安装包]
comments: true
author: admin
---
# arm64 Docker 安装包

本仓库提供了一个适用于 Linux arm64 架构的 Docker 和 Docker Compose 一键安装包。通过运行 `install.sh` 脚本，您可以轻松完成 Docker 和 Docker Compose 的安装。

## 资源文件描述

- **文件名称**: arm64 docker安装包
- **文件内容**: 包含 Docker 和 Docker Compose 的安装脚本 `install.sh`。
- **Docker 版本**: 19.03.9

## 使用方法

1. 下载本仓库中的 `install.sh` 脚本。
2. 在您的 Linux arm64 系统上运行以下命令：

   ```bash
   chmod +x install.sh
   ./install.sh
   ```

3. 脚本将自动完成 Docker 和 Docker Compose 的安装。

## 注意事项

- 请确保您的系统是基于 arm64 架构的 Linux 系统。
- 在运行脚本之前，建议备份重要数据，以防安装过程中出现意外情况。

## 支持与反馈

如果您在使用过程中遇到任何问题或有任何建议，欢迎通过仓库的 Issues 功能提出。我们将尽力为您提供帮助。

## 下载链接

[arm64Docker安装包](https://pan.quark.cn/s/b59c06a93d88)